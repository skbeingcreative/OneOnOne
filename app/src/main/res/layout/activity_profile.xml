<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/grey_900"
    tools:context="com.syberkeep.oneonone.ProfileActivity">

    <ImageView
        android:id="@+id/profile_user_image"
        android:src="@drawable/default_avatar"
        android:scaleType="centerCrop"
        android:layout_width="match_parent"
        android:layout_height="250dp" />

    <TextView
        android:id="@+id/profile_display_text"
        android:layout_below="@id/profile_user_image"
        android:text="Display Name"
        android:textAlignment="center"
        android:textSize="22sp"
        android:textStyle="bold"
        android:padding="15dp"
        android:textColor="@color/white"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/profile_status_text"
        android:layout_below="@id/profile_display_text"
        android:text="status here..."
        android:textAlignment="center"
        android:textSize="18sp"
        android:padding="5dp"
        android:textColor="@color/white"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/decline_friend_req_btn"
        android:text="Decline Friend Request"
        android:padding="18dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_margin="8dp"
        android:backgroundTint="@color/grey_500"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <Button
        android:id="@+id/send_friend_req_btn"
        android:text="Send Friend Request"
        android:padding="18dp"
        android:layout_centerHorizontal="true"
        android:layout_above="@id/decline_friend_req_btn"
        style="@style/Base.Widget.AppCompat.Button.Colored"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" />

    <TextView
        android:id="@+id/profile_friends_count_text"
        android:layout_above="@id/send_friend_req_btn"
        android:text="Total friends"
        android:textAlignment="center"
        android:textSize="15sp"
        android:padding="10dp"
        android:textColor="@color/white"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</RelativeLayout>